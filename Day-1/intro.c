// Program in a C language to used the getchar(), putchar(),
#include<stdio.h>
int main(){
    char ch;
    printf("Enter the Character You want to display: ");
    ch = getchar();
    putchar(ch);
    return 0;
}